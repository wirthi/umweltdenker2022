<% provide(:title, 'Alle Beiträge') %>
<h1>
  Alle Beiträge
  <%= @category.title if @category %>
</h1>

<%= will_paginate %>

<% if @contributions.count > 0 %>
  <ul class="users">
    <%= render @contributions %>
  </ul>
<% elsif @category %>
  <p>Es sind noch keine Beiträge in der Kategorie <%= @category.title %> angelegt.</p>
<% else %>
  <p>Es sind noch keine Beiträge angelegt.</p>
<% end %>

<%= will_paginate %>