<%= link_to gravatar_for(current_user, size: 50), current_user %>
<h1><%= current_user.firstname %> <%= current_user.name %></h1>
<span><%= link_to "Mein Profil anzeigen", current_user %></span>
<span><%= current_user.contributions.count %> Beiträge</span>